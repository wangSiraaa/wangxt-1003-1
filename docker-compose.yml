version: '3.8'

services:
  after-sales-app:
    build: .
    container_name: after-sales-collaboration
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
